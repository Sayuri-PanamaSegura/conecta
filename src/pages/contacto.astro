---
import Layout from "../layouts/Layout.astro";
import { ArrowLeft, User, Mail, MessageSquare, Phone, Building2, CircleHelp } from "lucide-react";

---
<Layout ocultarSuscripcion={true}>
  <!-- tu contenido de contacto aquí -->
   
   <section id="contacto" style="background-color: #FAFAFA" class="py-24 px-6">
    <div class="max-w-3xl mx-auto text-center mb-12 relative">
  <a 
    href="/" 
    class="absolute left-0 -top-2 inline-flex items-center justify-center w-10 h-9 rounded-full border border-black text-[#800080] hover:bg-[#800080] hover:text-white transition"
    aria-label="Regresar al inicio"
  >
    <ArrowLeft class="w-4 h-4" />
  </a>


    <div class="max-w-3xl mx-auto text-center mb-12">
      <h1 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900">Contáctanos</h1>
      <p class="text-gray-600 text-base md:text-lg">
        ¿Tienes dudas o quieres trabajar con nosotros? Déjanos un mensaje y te responderemos lo antes posible.
      </p>
    </div>

  <form 
    action="https://formsubmit.co/tu-correo@ejemplo.com" 
    method="POST"
    class="max-w-2xl mx-auto bg-white p-10 rounded-3xl shadow-xl grid gap-6"
  >
    <input type="hidden" name="_captcha" value="false">
    <input type="hidden" name="_next" value="/gracias">

    <!-- Nombre y Apellido -->
    <div class="grid md:grid-cols-2 gap-4">
      <div>
        <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
          <User class="w-5 h-5 text-purple-500" /> Nombre
        </label>
        <input type="text" name="nombre" required placeholder="Ej. María" class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-600 transition" />
      </div>
      <div>
        <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
          <User class="w-5 h-5 text-purple-500" /> Apellido
        </label>
        <input type="text" name="apellido" required placeholder="Ej. López" class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-600 transition" />
      </div>
    </div>

    <!-- Correo -->
    <div>
      <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
        <Mail class="w-5 h-5 text-purple-500" /> Correo electrónico
      </label>
      <input type="email" name="email" required placeholder="tucorreo@email.com" class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-600 transition" />
    </div>

    <!-- Teléfono o WhatsApp -->
    <div>
      <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
        <Phone class="w-5 h-5 text-purple-500" /> Teléfono / WhatsApp
      </label>
      <input type="tel" name="telefono" placeholder="Ej. +52 55 1234 5678" class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-600 transition" />
    </div>

    <!-- Empresa -->
    <div>
      <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
        <Building2 class="w-5 h-5 text-purple-500" /> Empresa
      </label>
      <input type="text" name="empresa" placeholder="Nombre de la empresa" class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-600 transition" />
    </div>

    <!-- Solución de interés -->
    <div>
      <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
        <CircleHelp class="w-5 h-5 text-purple-500" /> Solución de tu interés
      </label>
      <select name="solucion" required class="w-full px-4 py-3 border border-gray-300 rounded-xl bg-white text-gray-800 focus:outline-none focus:ring-2 focus:ring-purple-600 transition">
        <option value="" disabled selected>Selecciona una opción</option>
        <option value="conecta-cripto">Conecta Cripto</option>
        <option value="tesseract">Tesseract</option>
        <option value="hermes">Hermes</option>
        <option value="google-cloud">Google Cloud Partner</option>
        <option value="otra">Otra</option>
      </select>
    </div>

    <!-- Mensaje -->
    <div>
      <label class="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
        <MessageSquare class="w-5 h-5 text-purple-500" /> ¿Cómo podemos ayudarte?
      </label>
      <textarea 
        name="mensaje" 
        rows="4" 
        placeholder="Describe brevemente tu necesidad o proyecto..." 
        class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-600 transition resize-none"
      ></textarea>
    </div>

    <!-- Botón -->
    <button 
      type="submit" 
      class="relative inline-block px-6 py-3 font-semibold text-black group shadow-md rounded-full overflow-hidden"
    >
      <span class="absolute inset-0 w-full h-full bg-[#F0F0F0] transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left rounded-full z-0"></span>
      <span class="relative z-10">Enviar Mensaje</span>
    </button>
  </form>
</section>

</Layout>

